[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  # Set VITE_BACKEND_URL in Netlify UI to your Render backend URL (e.g. https://your-backend.onrender.com)
  # VITE_BACKEND_URL = "https://your-backend.onrender.com"
